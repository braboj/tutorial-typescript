// Prettier configuration. printWidth is pinned to 80 so every example file stays
// within the tutorial's line-length budget. The other options are Prettier's
// defaults, written out explicitly for clarity.
/** @type {import("prettier").Config} */
export default {
  printWidth: 80,
  tabWidth: 2,
  semi: true,
  singleQuote: false,
  trailingComma: "all",
};
