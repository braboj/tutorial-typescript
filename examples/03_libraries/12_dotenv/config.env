# Sample environment file loaded by 01_dotenv.ts.
# Normally this file is named ".env" and is git-ignored; we use "config.env"
# here only so it can live in the tutorial repo.
APP_NAME=ts-sprint
PORT=8080
FEATURE_NEW_UI=true
