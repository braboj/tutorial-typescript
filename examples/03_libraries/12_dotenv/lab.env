# Sample environment file for the dotenv lab.
# Normally this file is named ".env" and is git-ignored; we use "lab.env"
# here only so it can live in the tutorial repo. Load it from lab.ts.
SERVICE_NAME=billing-api
TIMEOUT_MS=2500
RETRIES=3
DEBUG_MODE=false
